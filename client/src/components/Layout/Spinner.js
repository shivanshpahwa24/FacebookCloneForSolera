import React, { Fragment } from "react";
const Spinner = () => {
  return (
    <Fragment>
      <i
        className="fa fa-loading"
        style={{ margin: "auto", display: "block" }}
      ></i>
    </Fragment>
  );
};

export default Spinner;
